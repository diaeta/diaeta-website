---
layout: layouts/base.njk
title: "Neuigkeiten & Ernährungsberatung | Diaeta Brüssel"
description: "Bleiben Sie informiert über die neuesten Nachrichten, wissenschaftliche Beratung und praktische Tipps zur Ernährung von Pierre Abou-Zeid, Ernährungsberater in Brüssel."
pagination:
  data: collections.neuigkeiten
  size: 6
  alias: items
permalink: "/de/neuigkeiten/{% if pagination.pageNumber > 0 %}seite/{{ pagination.pageNumber + 1 }}/{% endif %}index.html"
lang: de
canonicalUrl: "https://diaeta.be/de/neuigkeiten/"
---

<section class="service-hero rd-hero">
    <div class="container">
        <div class="rd-hero-content">
            <span class="hero-badge rd-hero-badge animate-on-scroll fade-in"><i class="fa-solid fa-newspaper hero-badge-icon"></i>Diaeta Neuigkeiten</span>
            <h1 class="animate-on-scroll fade-up">Unsere Artikel & Beratung</h1>
            <p class="lead animate-on-scroll fade-up delay-200">
                Tauchen Sie ein in das Herz der Ernährungswissenschaft mit den Einblicken von Pierre Abou-Zeid. Praktische Tipps, tiefgreifende Analysen und die neuesten Trends zur Optimierung Ihrer Gesundheit.
            </p>
        </div>
    </div>
</section>

<section class="content-section actualites-listing-section bg-white">
    <div class="container">
        <div class="row">
            <div class="col-lg-9">
                {% if items.length > 0 %}
                <div class="row g-4">
                    {% for post in items %}
                    <div class="col-md-6 d-flex">
                        <article class="article-teaser-card animate-on-scroll fade-up w-100">
                            {% if post.data.image %}
                            <a href="{{ post.url | url }}" class="teaser-card-image-link" aria-hidden="true" tabindex="-1">
                                <img src="{{ post.data.image | url }}" alt="{{ post.data.imageAlt | default(post.data.title) }}" class="teaser-card-image" loading="lazy">
                            </a>
                            {% endif %}
                            <div class="teaser-card-body">
                                {% if post.data.category %}
                                    <span class="teaser-card-category"><a href="{{ ('/de/nachrichten/kategorien/' + post.data.category | slugify + '/') | url }}">{{ post.data.category }}</a></span>
                                {% endif %}
                                <h3 class="teaser-card-title h5"><a href="{{ post.url | url }}">{{ post.data.title }}</a></h3>
                                {% set excerpt = post.data.description %}
                                {% if not excerpt and post.templateContent %}{% set excerpt = post.templateContent | striptags | truncate(100) %}{% elif excerpt %}{% set excerpt = excerpt | truncate(100) %}{% endif %}
                                {% if excerpt %}<p class="teaser-card-excerpt">{{ excerpt }}</p>{% endif %}
                                <div class="teaser-card-meta">
                                     {% if post.data.date %}<span class="teaser-card-date"><i class="fa-regular fa-calendar"></i> {{ post.date | date }}</span>{% endif %}
                                    <a href="{{ post.url | url }}" class="btn btn-sm btn-outline-primary teaser-card-readmore">Weiterlesen <i class="fa-solid fa-arrow-right fa-xs"></i></a>
                                </div>
                            </div>
                        </article>
                    </div>
                    {% endfor %}
                </div>

                {# Pagination Navigation #}
                {% if pagination.pages.length > 1 %}
                <nav aria-label="Neuigkeiten Navigation" class="mt-5 pt-4 border-top">
                    <ul class="pagination justify-content-center">
                        {% if pagination.href.previous %}<li class="page-item"><a class="page-link" href="{{ pagination.href.previous | url }}">Vorherige</a></li>
                        {% else %}<li class="page-item disabled"><span class="page-link">Vorherige</span></li>{% endif %}
                        {% for pageLink in pagination.hrefs %}<li class="page-item {% if page.url == pageLink %}active{% endif %}" aria-current="{% if page.url == pageLink %}page{% endif %}"><a class="page-link" href="{{ pageLink | url }}">{{ loop.index }}</a></li>{% endfor %}
                        {% if pagination.href.next %}<li class="page-item"><a class="page-link" href="{{ pagination.href.next | url }}">Nächste</a></li>
                        {% else %}<li class="page-item disabled"><span class="page-link">Nächste</span></li>{% endif %}
                    </ul>
                </nav>
                {% endif %}

                {% else %}
                <p class="text-center"><em>Noch keine Artikel veröffentlicht. Schauen Sie bald wieder vorbei!</em></p>
                {% endif %}
            </div>

            <aside class="col-lg-3 mt-5 mt-lg-0">
                <div class="sidebar-widget">
                    <h4 class="sidebar-title">Kategorien</h4>
                    {% if collections.uniqueCategories.length > 0 %}
                        <ul class="list-unstyled category-list">
                            {% for category in collections.uniqueCategories %}
                            <li>
                                <a href="{{ ('/de/nachrichten/kategorien/' + category | slugify + '/') | url }}">{{ category }} 
                                    <span class="badge bg-primary rounded-pill">
                                        {{ collections.postsByCategory[category].length }}
                                    </span>
                                </a>
                            </li>
                            {% endfor %}
                        </ul>
                    {% else %}
                        <p><em>Noch keine Kategorien vorhanden.</em></p>
                    {% endif %}
                </div>
            </aside>
        </div>
    </div>
</section>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "Neuigkeiten & Ernährungsberatung | Diaeta Brüssel",
  "description": "Bleiben Sie informiert über die neuesten Nachrichten, wissenschaftliche Beratung und praktische Tipps zur Ernährung von Pierre Abou-Zeid, Ernährungsberater in Brüssel. Artikel über Gewichtsverlust, Reizdarmsyndrom, Diabetes und mehr.",
  "url": "https://diaeta.be{{ canonicalUrl }}",
  "publisher": {
    "@id": "https://diaeta.be#organization"
  },
  "mainEntity": {
    "@type": "ItemList",
    "itemListElement": [
      {% for post in items %}
      {
        "@type": "ListItem",
        "position": {{ loop.index + (pagination.pageNumber * pagination.size) }},
        "url": "https://diaeta.be{{ post.url | url }}",
        "name": "{{ post.data.title | escape }}"
      }{% if not loop.last %},{% endif %}
      {% endfor %}
    ]
  }
}
</script>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Diaeta Startseite",
      "item": "https://diaeta.be/de/index.html"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "Neuigkeiten",
      "item": "https://diaeta.be/de/neuigkeiten/"
    }
  ]
}
</script>
