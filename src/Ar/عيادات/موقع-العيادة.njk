---
layout: layouts/base.njk
pagination:
  data: cabinets_ar
  size: 1
  alias: clinic
permalink: "/ar/عيادة/{{ clinic.id }}/index.html"
eleventyComputed:
  title: "{{ clinic.name }} - استشارة التغذية دياتا | {{ clinic.city }}"
  description: "احجز موعداً مع بيار أبو زيد في عيادة {{ clinic.name }} في {{ clinic.city }}. استشارة تغذية مهنية وعلاج شخصي."
  clinic_name: "{{ clinic.name }}"
lang: ar
---

<section class="service-hero rd-hero" dir="rtl" style="text-align: right;">
    <div class="container">
        <div class="rd-hero-content">
            <span class="hero-badge rd-hero-badge animate-on-scroll fade-in"><i class="fa-solid fa-map-marker-alt hero-badge-icon"></i>عيادة دياتا</span>
            <h1 class="animate-on-scroll fade-up">{{ clinic.name }}</h1>
            <p class="lead animate-on-scroll fade-up delay-200">
                استشارة التغذية مع بيار أبو زيد في {{ clinic.city }}
            </p>
        </div>
    </div>
</section>

<section class="content-section bg-white" dir="rtl" style="text-align: right;">
    <div class="container">
        <div class="row">
            <div class="col-lg-8">
                <div class="clinic-details">
                    <h2>العنوان والاتصال</h2>
                    <div class="clinic-info-card">
                        <div class="clinic-info-header">
                            <h3>{{ clinic.name }}</h3>
                            <div class="clinic-address">
                                <i class="fa-solid fa-map-marker-alt"></i>
                                <div>
                                    <p>{{ clinic.fullAddress }}</p>
                                    {% if clinic.Maps_link %}
                                    <a href="{{ clinic.Maps_link }}" target="_blank" rel="noopener noreferrer" class="btn btn-sm btn-outline-primary">
                                        <i class="fa-solid fa-external-link-alt"></i> عرض الطريق
                                    </a>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                        {% if clinic.notes %}
                        <div class="clinic-notes">
                            <i class="fa-solid fa-info-circle"></i>
                            <p>{{ clinic.notes }}</p>
                        </div>
                        {% endif %}

                        {% if clinic.opening_hours %}
                        <div class="clinic-hours">
                            <h4><i class="fa-solid fa-clock"></i> ساعات العمل</h4>
                            <ul class="hours-list">
                                {% for day in clinic.opening_hours %}
                                <li>
                                    <strong>{{ day.dayOfWeekFRENCH }}:</strong>
                                    {% for slot in day.timeSlots %}
                                        {{ slot.opens }} - {{ slot.closes }}{% if not loop.last %}، {% endif %}
                                    {% endfor %}
                                </li>
                                {% endfor %}
                            </ul>
                            {% if clinic.hours_details_note %}
                            <p class="hours-note"><em>{{ clinic.hours_details_note }}</em></p>
                            {% endif %}
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="clinic-cta-sidebar">
                    <div class="cta-card">
                        <h3>حجز موعد</h3>
                        <p>احجز موعد استشارتك مع بيار أبو زيد</p>
                        {% if clinic.doctorpracticeId %}
                        <a href="https://app.doctorpractice.be/profile/{{ clinic.doctorpracticeId }}" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg w-100 mb-3">
                            <i class="fa-solid fa-calendar-check"></i> حجز أونلاين
                        </a>
                        {% endif %}
                        <a href="tel:+32479355551" class="btn btn-outline-primary w-100" style="text-align: center;">
                            <i class="fa-solid fa-phone"></i> +٣٢ ٤٧٩ ٣٥ ٥٥ ٥١
                        </a>
                    </div>

                    <div class="clinic-services">
                        <h4>خدماتنا</h4>
                        <ul>
                            <li><a href="/ar/فقدان-الوزن/">فقدان الوزن</a></li>
                            <li><a href="/ar/متلازمة-القولون-العصبي/">متلازمة القولون العصبي (FODMAP)</a></li>
                            <li><a href="/ar/السكري-النوع-الثاني/">السكري النوع الثاني</a></li>
                            <li><a href="/ar/ضغط-الدم-الكوليسترول/">ضغط الدم والكوليسترول</a></li>
                            <li><a href="/ar/الاختبارات-الغذائية-الجينية/">الاختبارات الغذائية الجينية</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{% if clinic.coordinates %}
<section class="clinic-map-section">
    <div class="container-fluid p-0">
        <div id="clinic-map" class="clinic-map" data-lat="{{ clinic.coordinates[0] }}" data-lng="{{ clinic.coordinates[1] }}" data-name="{{ clinic.name }}"></div>
    </div>
</section>
{% endif %}
